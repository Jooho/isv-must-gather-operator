apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  labels:
    bin: isv-cli
  name: must-gather-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
